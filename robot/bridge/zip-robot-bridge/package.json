{
  "name": "zip-robot-bridge",
  "version": "1.0.0",
  "description": "Bridge service for ZIP robot - translates WebSocket to serial binary protocol",
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "build:local": "tsc",
    "start": "node dist/index.js",
    "dev": "tsx watch src/index.ts",
    "dev:local": "tsx watch src/index.ts",
    "typecheck": "tsc --noEmit",
    "test:health": "node -e \"fetch('http://localhost:8766/health').then(r => r.json()).then(d => console.log(JSON.stringify(d, null, 2))).catch(e => console.error('Health check failed:', e.message))\"",
    "test:commands": "tsx scripts/test-all-commands.ts"
  },
  "dependencies": {
    "serialport": "^13.0.0",
    "ws": "^8.18.0",
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "@types/ws": "^8.5.10",
    "tsx": "^4.7.0",
    "typescript": "^5.3.3"
  }
}

