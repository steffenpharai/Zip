# ZIP - Jarvis-style HUD Assistant
# Environment Variables Configuration

# =============================================================================
# REQUIRED - OpenAI API Configuration
# =============================================================================

# Your OpenAI API key (required for all AI features)
# Get your key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-api-key-here

# =============================================================================
# OPTIONAL - OpenAI Model Configuration
# =============================================================================

# Realtime model for voice interactions (WebRTC)
# Default: gpt-4o-realtime-preview-2024-12-17
OPENAI_REALTIME_MODEL=gpt-4o-realtime-preview-2024-12-17

# Responses model for chat and tool calling (LangGraph orchestration)
# Default: gpt-4o
OPENAI_RESPONSES_MODEL=gpt-4o

# Vision model for image analysis
# Default: gpt-4o
OPENAI_VISION_MODEL=gpt-4o

# TTS model for voice fallback (text-to-speech)
# Default: tts-1
# Options: tts-1, tts-1-hd
OPENAI_TTS_MODEL=gpt-4o-mini-tts-2025-12-15

# STT model for voice fallback (speech-to-text)
# Default: whisper-1
OPENAI_STT_MODEL=whisper-1

# =============================================================================
# OPTIONAL - ZIP Feature Flags
# =============================================================================

# Enable Realtime WebRTC voice integration
# Set to False to disable Realtime (will use fallback STT/TTS)
# Default: true
ZIP_REALTIME_ENABLED=true

# Enable STT/TTS fallback voice pipeline
# Set to False to disable voice fallback entirely
# Default: true
ZIP_VOICE_FALLBACK_ENABLED=true

# Panel update interval in milliseconds
# How often the left rail panels (system stats, weather, uptime) refresh
# Default: 2000 (2 seconds)
ZIP_UPDATE_INTERVAL_MS=2000

# =============================================================================
# NOTES
# =============================================================================
#
# 1. Copy this file to .env and fill in your OPENAI_API_KEY
# 2. All other variables are optional and have sensible defaults
# 3. For production, consider using a secrets manager instead of .env files
# 4. Weather API (Open-Meteo) requires no API key - it's free and open-source
#
# =============================================================================
